# Database (SQLite file in backend folder)
DATABASE_URL="file:./dev.db"

# JWT secret for auth tokens (change in production)
JWT_SECRET="ecommerce-secret"

# Optional: API port (default 3001)
# PORT=3001
